export default function Create() {
  return (
    <section className="space-y-6">
      <header className="space-y-1">
        <h1 className="text-2xl font-medium tracking-tight">
          wallet <span className="text-muted-foreground">// create</span>
        </h1>
        <p className="text-sm text-muted-foreground">
          Generate a fresh keypair locally. Download the keystore and import it into MetaMask before using it on /buy or /return.
        </p>
      </header>
      <p className="text-sm text-muted-foreground">// T5</p>
    </section>
  )
}
